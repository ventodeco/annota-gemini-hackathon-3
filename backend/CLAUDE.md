# Backend Claude Guidance

Use [`AGENTS.md`](AGENTS.md) in this directory as the canonical backend guidance. It contains the current Go, PostgreSQL, Redis, configuration, JSON API, testing, and concurrency rules.

Do not duplicate backend coding rules here; keeping one source of truth prevents stale database and environment examples.
